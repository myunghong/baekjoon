#include<algorithm>
#include<queue>
#include<iostream>
using namespace std;
#define MAX 1000000
int main(void){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int n;
    int suyeol[MAX];
    cin >> n;
    for(int i = 0; i<n; i++){
        cin >> suyeol[i];
    }
    return 0;
}