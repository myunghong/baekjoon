#include<algorithm>
#include<iostream>
using namespace std;
#define MAX 100005
int board[MAX];
int main(void){
    int n, en = 0, st = 0; cin >> n;
    for(int i = 0;i <n; i++){
        cin >> board[i];
    }
    while(en < n && st < n){
        
    }

    return 0;
}