#include<algorithm>
#include<iostream>
using namespace std;
int board[200005];
int check[100005];
int main(void){
    cin.tie(0);
    ios::sync_with_stdio(0);
    int n, k; cin >> n >> k; //n은 수열의 수, k는 겹쳐도 되는 개수
    for(int i = 0; i<n; i++){
        cin >> board[i];
    }
    for(int i = 0; i<n; i++){
        check[board[i]]++;
        while(en < st){
            
        }
    }
    return 0;
}