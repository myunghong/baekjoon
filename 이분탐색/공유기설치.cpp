#include<algorithm>
#include<iostream>
using namespace std;
int house[200005];
int main(void){
    int n,c; cin >> n >> c; //n은 집 수, c는 공유기 수
    for(int i = 0; i<n; i++){
        cin >> house[i];
    }
    sort(house, house + n);
    int st = 0, en = 
    while(true){
        int cnt = 0;
        for(){
            if(cnt )
        }
        if(cnt == house[n-1]/(c-1)) break;
    }

    return 0;
}