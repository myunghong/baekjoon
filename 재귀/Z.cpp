#include<algorithm>
#include<iostream>
using namespace std;
long long ans = 0;
int r,c;
void recursion(int n){
    if(n == 1){
        
    }
}
int main(void){
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n; cin >> n >> r >> c;
    recursion(n);
    cout << ans;

    return 0;
}